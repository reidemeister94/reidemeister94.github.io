// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  // GitHub Pages user site — served at the repo's root domain.
  // If you later add a custom domain, change this to e.g. "https://yourdomain.com".
  site: "https://reidemeister94.github.io",
});
